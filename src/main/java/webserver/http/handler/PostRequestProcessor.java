package webserver.http.handler;

import java.io.DataOutputStream;
import java.net.URI;

public class PostRequestProcessor implements RequestProcessor{

    @Override
    public void processing(DataOutputStream dataOutputStream, URI requestUri) {

    }
}
